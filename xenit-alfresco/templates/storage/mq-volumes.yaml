{{- if and (.Values.mq.enabled) (not .Values.persistentStorage.mq.selfManaged) }}
{{- $namespace := .Release.Namespace -}}
{{- with .Values.persistentStorage.mq }}
{{- if .enabled}}
{{- $name := "mq" -}}
{{- $storageClassName := .storageClassName -}}
{{- $storage := .storage -}}
{{- $efsVolumeHandle := .efs.volumeHandle -}}
{{- include "hepers.volumeHelper" (list $namespace $name $storageClassName $storage $efsVolumeHandle) }}
{{- end }}
{{- end }}
{{- end }}